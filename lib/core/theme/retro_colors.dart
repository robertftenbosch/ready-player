import 'package:flutter/material.dart';

class RetroColors {
  RetroColors._();

  // Base
  static const background = Color(0xFF0D0D0D);
  static const surface = Color(0xFF1A1A2E);
  static const surfaceLight = Color(0xFF252540);

  // Primary - Terminal Green
  static const primary = Color(0xFF00FF41);
  static const primaryDim = Color(0xFF00AA2A);
  static const primaryGlow = Color(0x4400FF41);

  // Secondary - Amber CRT
  static const secondary = Color(0xFFFF6600);
  static const secondaryDim = Color(0xFFAA4400);

  // Accent - Red
  static const accent = Color(0xFFFF0040);
  static const accentDim = Color(0xFFAA002A);

  // Text
  static const textPrimary = Color(0xFF00FF41);
  static const textSecondary = Color(0xFF00CC33);
  static const textMuted = Color(0xFF007A1F);

  // Chess board
  static const boardLight = Color(0xFF6B8E23);
  static const boardDark = Color(0xFF2E4600);

  // Checkers board
  static const checkersLight = Color(0xFFD4A574);
  static const checkersDark = Color(0xFF5C3A1E);

  // Pieces
  static const pieceWhite = Color(0xFFE0E0E0);
  static const pieceBlack = Color(0xFF3A3A3A);

  // Ballerburg
  static const skyTop = Color(0xFF000033);
  static const skyBottom = Color(0xFF000066);
  static const mountain = Color(0xFF2E4600);
  static const castle = Color(0xFF8B8B83);
  static const castleDamaged = Color(0xFF5C4033);
  static const cannonBall = Color(0xFFFFCC00);
  static const explosion = Color(0xFFFF4500);
  static const wind = Color(0xFF87CEEB);
}
